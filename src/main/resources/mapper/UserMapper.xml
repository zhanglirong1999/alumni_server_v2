<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="cn.edu.seu.alumni_server.dao.mapper.UserMapper">
    <resultMap id="BaseResultMap" type="cn.edu.seu.alumni_server.dao.entity.User">
        <!--
          WARNING - @mbg.generated
        -->
        <result column="openid" jdbcType="VARCHAR" property="openid"/>
        <result column="phone" jdbcType="VARCHAR" property="phone"/>
        <result column="wechat" jdbcType="VARCHAR" property="wechat"/>
        <result column="email" jdbcType="VARCHAR" property="email"/>
    </resultMap>

    <select id="checkExsist"
    resultType="cn.edu.seu.alumni_server.controller.dto.WorkDTO">
    select a.openid,b.company,b.job,b.start_year,b.end_year,c.id
      from user a,user_work b,user_info c
    where a.openid=#{id} and a.openid=b.openid and a.openid = c.openid
  </select>
</mapper>